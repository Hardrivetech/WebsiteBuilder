<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= site.siteName %></title>
    <style>
      :root {
          --primary-color: <%= site.theme === 'dark' ? '#00aaff' : '#007bff' %>;
          --background-color: <%= site.theme === 'dark' ? '#121212' : '#f8f9fa' %>;
          --text-color: <%= site.theme === 'dark' ? '#e1e1e1' : '#212529' %>;
          --card-bg: <%= site.theme === 'dark' ? '#1e1e1e' : '#ffffff' %>;
      }

      body {
          font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
          margin: 0;
          background-color: var(--background-color);
          color: var(--text-color);
          display: flex;
          justify-content: center;
          align-items: center;
          min-height: 100vh;
          text-align: center;
          padding: 2rem;
      }

      .container {
          max-width: 600px;
          padding: 3rem;
          background-color: var(--card-bg);
          border-radius: 8px;
          box-shadow: 0 4px 8px rgba(0,0,0,0.1);
      }

      h1 {
          color: var(--primary-color);
          font-size: 2.5rem;
          margin-bottom: 0.5rem;
      }

      p {
          font-size: 1.25rem;
          line-height: 1.6;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1><%= site.siteName %></h1>
      <p><%= site.headline %></p>
    </div>
  </body>
</html>
